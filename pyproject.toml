# pyproject.toml
[build-system]
# Setuptools (or your chosen backend) and Wheel are required for the build process.
# Add numpy to this list if your setup.py/setup.cfg requires it for configuration or building.
requires = [
    "setuptools>=61.0.0",
    "wheel",
    "numpy",
]
build-backend = "setuptools.build_meta"
